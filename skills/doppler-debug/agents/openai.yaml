interface:
  display_name: "Doppler Debug"
  short_description: "Diagnose a named Doppler failure"
  default_prompt: "Use $doppler-debug to reproduce and diagnose this failure in Doppler; repair it only if requested."
