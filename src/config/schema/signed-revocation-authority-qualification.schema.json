{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://clocksmith.dev/doppler/schemas/signed-revocation-authority-qualification-v3.schema.json",
  "title": "Doppler Signed Revocation Authority Qualification v3",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "schemaVersion",
    "source",
    "goalId",
    "minimumQualifiedAuthorities",
    "ownerConfirmationMaxAgeDays",
    "evidenceMaxAgeDays",
    "requiredHosts",
    "requiredDrills",
    "authorities"
  ],
  "properties": {
    "$schema": {
      "const": "../../src/config/schema/signed-revocation-authority-qualification.schema.json"
    },
    "schemaVersion": { "const": 3 },
    "source": { "const": "doppler" },
    "goalId": { "const": "evidence-backed-correctness-performance" },
    "minimumQualifiedAuthorities": { "const": 1 },
    "ownerConfirmationMaxAgeDays": {
      "type": "integer",
      "minimum": 1,
      "maximum": 365
    },
    "evidenceMaxAgeDays": {
      "type": "integer",
      "minimum": 1,
      "maximum": 365
    },
    "requiredHosts": {
      "const": ["browser", "node"]
    },
    "requiredDrills": {
      "const": [
        "refresh-current",
        "online-key-rotation",
        "exact-replay",
        "rewritten-replay-rejection",
        "sequence-rollback-rejection",
        "epoch-rollback-rejection",
        "offline-expiry",
        "compromise-recovery",
        "durable-store-restart",
        "loaded-identity-invalidation",
        "application-fail-closed"
      ]
    },
    "authorities": {
      "type": "array",
      "items": { "$ref": "#/$defs/authority" }
    }
  },
  "$defs": {
    "nullableString": {
      "type": ["string", "null"],
      "minLength": 1
    },
    "nullableDateTime": {
      "type": ["string", "null"],
      "format": "date-time"
    },
    "nullableRevision": {
      "anyOf": [
        { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        { "type": "null" }
      ]
    },
    "nullableDigest": {
      "anyOf": [
        { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
        { "type": "null" }
      ]
    },
    "evidenceRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "digest"],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "not": {
            "anyOf": [
              { "pattern": "^/" },
              { "pattern": "\\\\" },
              { "pattern": "(^|/)\\.\\.(/|$)" }
            ]
          }
        },
        "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" }
      }
    },
    "nullableEvidenceRef": {
      "anyOf": [
        { "$ref": "#/$defs/evidenceRef" },
        { "type": "null" }
      ]
    },
    "durableStateStoreIds": {
      "type": "object",
      "additionalProperties": false,
      "required": ["browser", "node"],
      "properties": {
        "browser": { "$ref": "#/$defs/nullableString" },
        "node": { "$ref": "#/$defs/nullableString" }
      }
    },
    "deployment": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "endpointUrl",
        "authorityId",
        "transportPolicy",
        "onlineKeyIds",
        "recoveryKeyIds",
        "durableStateStoreIds"
      ],
      "properties": {
        "endpointUrl": { "$ref": "#/$defs/nullableString" },
        "authorityId": { "$ref": "#/$defs/nullableString" },
        "transportPolicy": { "const": "https-no-redirect" },
        "onlineKeyIds": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "recoveryKeyIds": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "durableStateStoreIds": { "$ref": "#/$defs/durableStateStoreIds" }
      }
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "ownerConfirmation",
        "endpointDeployment",
        "packageTrustBinding",
        "onlineKeyCustody",
        "recoveryKeyCustody",
        "custodySeparation",
        "browserDurableState",
        "nodeDurableState",
        "refreshCurrent",
        "onlineKeyRotation",
        "exactReplay",
        "rewrittenReplayRejection",
        "sequenceRollbackRejection",
        "epochRollbackRejection",
        "offlineExpiry",
        "compromiseRecovery",
        "durableStoreRestart",
        "loadedIdentityInvalidation",
        "applicationFailClosed",
        "requalification",
        "promotion"
      ],
      "properties": {
        "ownerConfirmation": { "$ref": "#/$defs/nullableEvidenceRef" },
        "endpointDeployment": { "$ref": "#/$defs/nullableEvidenceRef" },
        "packageTrustBinding": { "$ref": "#/$defs/nullableEvidenceRef" },
        "onlineKeyCustody": { "$ref": "#/$defs/nullableEvidenceRef" },
        "recoveryKeyCustody": { "$ref": "#/$defs/nullableEvidenceRef" },
        "custodySeparation": { "$ref": "#/$defs/nullableEvidenceRef" },
        "browserDurableState": { "$ref": "#/$defs/nullableEvidenceRef" },
        "nodeDurableState": { "$ref": "#/$defs/nullableEvidenceRef" },
        "refreshCurrent": { "$ref": "#/$defs/nullableEvidenceRef" },
        "onlineKeyRotation": { "$ref": "#/$defs/nullableEvidenceRef" },
        "exactReplay": { "$ref": "#/$defs/nullableEvidenceRef" },
        "rewrittenReplayRejection": { "$ref": "#/$defs/nullableEvidenceRef" },
        "sequenceRollbackRejection": { "$ref": "#/$defs/nullableEvidenceRef" },
        "epochRollbackRejection": { "$ref": "#/$defs/nullableEvidenceRef" },
        "offlineExpiry": { "$ref": "#/$defs/nullableEvidenceRef" },
        "compromiseRecovery": { "$ref": "#/$defs/nullableEvidenceRef" },
        "durableStoreRestart": { "$ref": "#/$defs/nullableEvidenceRef" },
        "loadedIdentityInvalidation": { "$ref": "#/$defs/nullableEvidenceRef" },
        "applicationFailClosed": { "$ref": "#/$defs/nullableEvidenceRef" },
        "requalification": { "$ref": "#/$defs/nullableEvidenceRef" },
        "promotion": { "$ref": "#/$defs/nullableEvidenceRef" }
      }
    },
    "authority": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "lifecycle",
        "owner",
        "ownerConfirmedAtUtc",
        "claimAllowed",
        "deployment",
        "harnessRevision",
        "environmentFingerprint",
        "qualifiedAtUtc",
        "expiresAtUtc",
        "evidence",
        "blockers"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
        },
        "lifecycle": {
          "enum": ["candidate", "active", "quarantined", "revoked", "retired"]
        },
        "owner": { "$ref": "#/$defs/nullableString" },
        "ownerConfirmedAtUtc": { "$ref": "#/$defs/nullableDateTime" },
        "claimAllowed": { "type": "boolean" },
        "deployment": { "$ref": "#/$defs/deployment" },
        "harnessRevision": { "$ref": "#/$defs/nullableRevision" },
        "environmentFingerprint": { "$ref": "#/$defs/nullableDigest" },
        "qualifiedAtUtc": { "$ref": "#/$defs/nullableDateTime" },
        "expiresAtUtc": { "$ref": "#/$defs/nullableDateTime" },
        "evidence": { "$ref": "#/$defs/evidence" },
        "blockers": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
