{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://clocksmith.dev/doppler/schemas/provider-conformance-policy-v3.schema.json",
  "title": "Doppler Provider Conformance Policy v3",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "schemaVersion",
    "source",
    "goalIds",
    "minimumQualifiedSuites",
    "requiredWorkloads",
    "coreProviderLaneIds",
    "qualificationMaxAgeDays",
    "requiredLifecycleStages",
    "requiredEvidenceFields",
    "providerLanes",
    "suites"
  ],
  "properties": {
    "$schema": { "const": "../../src/config/schema/provider-conformance-policy.schema.json" },
    "schemaVersion": { "const": 3 },
    "source": { "const": "doppler" },
    "goalIds": {
      "const": ["model-artifact-runtime-contract", "correctness-performance-claims"]
    },
    "minimumQualifiedSuites": { "const": 3 },
    "requiredWorkloads": { "const": ["generation", "embedding", "reranking"] },
    "coreProviderLaneIds": { "const": ["browser-webgpu", "node-webgpu"] },
    "qualificationMaxAgeDays": { "type": "integer", "minimum": 1, "maximum": 365 },
    "requiredLifecycleStages": { "const": ["load", "execute", "unload"] },
    "requiredEvidenceFields": {
      "const": [
        "modelContract",
        "resolutionIdentity",
        "operations",
        "lifecycle",
        "correctness",
        "providerReceipt",
        "promotion"
      ]
    },
    "providerLanes": {
      "type": "array",
      "minItems": 2,
      "items": { "$ref": "#/$defs/providerLane" }
    },
    "suites": {
      "type": "array",
      "items": { "$ref": "#/$defs/suite" }
    }
  },
  "$defs": {
    "id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
    "nullableString": {
      "anyOf": [{ "type": "string", "minLength": 1 }, { "type": "null" }]
    },
    "nullableSha256": {
      "anyOf": [
        { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
        { "type": "null" }
      ]
    },
    "nullableDateTime": {
      "anyOf": [{ "type": "string", "format": "date-time" }, { "type": "null" }]
    },
    "nullableRepoPath": {
      "anyOf": [
        {
          "type": "string",
          "minLength": 1,
          "not": {
            "anyOf": [
              { "pattern": "^/" },
              { "pattern": "\\\\" },
              { "pattern": "(^|/)\\.\\.(/|$)" }
            ]
          }
        },
        { "type": "null" }
      ]
    },
    "evidenceReference": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "digest"],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "not": {
            "anyOf": [
              { "pattern": "^/" },
              { "pattern": "\\\\" },
              { "pattern": "(^|/)\\.\\.(/|$)" }
            ]
          }
        },
        "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" }
      }
    },
    "nullableEvidenceReference": {
      "anyOf": [{ "$ref": "#/$defs/evidenceReference" }, { "type": "null" }]
    },
    "providerLane": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "role", "contractPath"],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "kind": { "enum": ["browser-webgpu", "node-webgpu", "doe", "other-webgpu"] },
        "role": { "enum": ["core", "optional-named"] },
        "contractPath": { "type": "string", "minLength": 1 }
      }
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "modelContract",
        "resolutionIdentity",
        "operations",
        "lifecycle",
        "correctness",
        "providerReceipt",
        "promotion"
      ],
      "properties": {
        "modelContract": { "$ref": "#/$defs/nullableEvidenceReference" },
        "resolutionIdentity": { "$ref": "#/$defs/nullableEvidenceReference" },
        "operations": { "$ref": "#/$defs/nullableEvidenceReference" },
        "lifecycle": { "$ref": "#/$defs/nullableEvidenceReference" },
        "correctness": { "$ref": "#/$defs/nullableEvidenceReference" },
        "providerReceipt": { "$ref": "#/$defs/nullableEvidenceReference" },
        "promotion": { "$ref": "#/$defs/nullableEvidenceReference" }
      }
    },
    "provider": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "laneId",
        "implementationId",
        "harnessRevision",
        "logicalModelId",
        "manifestVariantId",
        "resolvedArtifactVariantId",
        "resolvedExecutionId",
        "environmentFingerprint",
        "operations",
        "lifecycle",
        "correctness",
        "qualifiedAtUtc",
        "expiresAtUtc",
        "evidence",
        "claimAllowed",
        "blockers"
      ],
      "properties": {
        "laneId": { "$ref": "#/$defs/id" },
        "implementationId": { "$ref": "#/$defs/nullableString" },
        "harnessRevision": {
          "anyOf": [
            { "type": "string", "pattern": "^[0-9a-f]{40}$" },
            { "type": "null" }
          ]
        },
        "logicalModelId": { "$ref": "#/$defs/nullableString" },
        "manifestVariantId": { "$ref": "#/$defs/nullableString" },
        "resolvedArtifactVariantId": { "$ref": "#/$defs/nullableSha256" },
        "resolvedExecutionId": { "$ref": "#/$defs/nullableSha256" },
        "environmentFingerprint": { "$ref": "#/$defs/nullableSha256" },
        "operations": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "lifecycle": {
          "type": "object",
          "additionalProperties": false,
          "required": ["load", "execute", "unload"],
          "properties": {
            "load": { "enum": ["passed", "failed", "not-run"] },
            "execute": { "enum": ["passed", "failed", "not-run"] },
            "unload": { "enum": ["passed", "failed", "not-run"] }
          }
        },
        "correctness": {
          "type": "object",
          "additionalProperties": false,
          "required": ["class", "passed"],
          "properties": {
            "class": {
              "enum": [
                "exact-token",
                "tolerance-bounded-numerical",
                "semantic",
                "held-out-task-metric"
              ]
            },
            "passed": { "type": "boolean" }
          }
        },
        "qualifiedAtUtc": { "$ref": "#/$defs/nullableDateTime" },
        "expiresAtUtc": { "$ref": "#/$defs/nullableDateTime" },
        "evidence": { "$ref": "#/$defs/evidence" },
        "claimAllowed": { "type": "boolean" },
        "blockers": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "suite": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "workload",
        "logicalModelId",
        "manifestVariantId",
        "resolvedArtifactVariantId",
        "workloadContractPath",
        "declaredOperations",
        "correctnessClass",
        "requiredProviderLaneIds",
        "promotion",
        "claimAllowed",
        "providers",
        "blockers"
      ],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "workload": { "enum": ["generation", "embedding", "reranking"] },
        "logicalModelId": { "$ref": "#/$defs/nullableString" },
        "manifestVariantId": { "$ref": "#/$defs/nullableString" },
        "resolvedArtifactVariantId": { "$ref": "#/$defs/nullableSha256" },
        "workloadContractPath": { "$ref": "#/$defs/nullableRepoPath" },
        "declaredOperations": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "correctnessClass": {
          "enum": [
            "exact-token",
            "tolerance-bounded-numerical",
            "semantic",
            "held-out-task-metric"
          ]
        },
        "requiredProviderLaneIds": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/id" }
        },
        "promotion": { "$ref": "#/$defs/nullableEvidenceReference" },
        "claimAllowed": { "type": "boolean" },
        "providers": {
          "type": "array",
          "items": { "$ref": "#/$defs/provider" }
        },
        "blockers": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
