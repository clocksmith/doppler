{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://clocksmith.dev/doppler/runtime-promotion-decision-evidence-v1.schema.json",
  "title": "Doppler Runtime Promotion Decision Evidence v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema", "promotionId", "candidateId", "candidateHash", "scope", "status",
    "decidedAtUtc", "reason", "revocationRecordId", "authority", "reviewer",
    "reviewerRevision", "statement"
  ],
  "properties": {
    "schema": { "const": "doppler.runtime-promotion-decision-evidence/v1" },
    "promotionId": { "$ref": "#/$defs/text" },
    "candidateId": { "$ref": "#/$defs/text" },
    "candidateHash": { "$ref": "#/$defs/digest" },
    "scope": { "$ref": "#/$defs/scope" },
    "status": { "enum": ["retain", "revoke"] },
    "decidedAtUtc": { "type": "string", "format": "date-time" },
    "reason": { "$ref": "#/$defs/text" },
    "revocationRecordId": { "type": ["string", "null"], "minLength": 1 },
    "authority": { "const": "human" },
    "reviewer": { "$ref": "#/$defs/text" },
    "reviewerRevision": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "statement": { "$ref": "#/$defs/text" }
  },
  "$defs": {
    "text": { "type": "string", "minLength": 1 },
    "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "scope": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "modelId", "artifactVariantId", "executionId", "providerId",
        "environmentFingerprint", "workloadId"
      ],
      "properties": {
        "modelId": { "$ref": "#/$defs/text" },
        "artifactVariantId": { "$ref": "#/$defs/digest" },
        "executionId": { "$ref": "#/$defs/digest" },
        "providerId": { "$ref": "#/$defs/text" },
        "environmentFingerprint": { "$ref": "#/$defs/text" },
        "workloadId": { "$ref": "#/$defs/text" }
      }
    }
  }
}
