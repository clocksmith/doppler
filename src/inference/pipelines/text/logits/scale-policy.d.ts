import type { LogitsConfig } from './types.js';

export declare function resolveLogitInputScale(config: LogitsConfig): number;
export declare function resolveLogitOutputScale(config: LogitsConfig): number;
