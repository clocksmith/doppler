{"id":"heldout-js-json-1","prompt":"Patch a JavaScript workload loader so optional metadata is preserved but missing required ids still throw.","completion":"Validate required ids before optional metadata normalization, copy optional metadata only when present, preserve null as explicit disable, and add a loader test for missing id plus preserved metadata."}
{"id":"heldout-js-json-2","prompt":"Review a JSON training workload that mixes benchmark fairness fields into lora config.","completion":"Move fairness fields into the eval or benchmark contract, keep lora config limited to adapter, dataset, freeze, trainer, export, and activation policy, and refresh the workload registry hash."}
