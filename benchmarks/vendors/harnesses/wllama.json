{
  "schemaVersion": 1,
  "id": "wllama",
  "name": "Wllama Harness",
  "execution": {
    "mode": "external-command",
    "stdoutFormat": "json",
    "defaultCommand": null,
    "examples": [
      "node tools/vendor-bench.js import --target wllama --input /tmp/wllama-result.json"
    ]
  },
  "normalization": {
    "metricPaths": {
      "decodeTokensPerSec": ["metrics.decodeTokensPerSec"],
      "prefillTokensPerSec": ["metrics.prefillTokensPerSec"],
      "firstTokenMs": ["metrics.firstTokenMs"],
      "decodeMsPerTokenP50": ["metrics.decodeMsPerTokenP50"],
      "decodeMsPerTokenP95": ["metrics.decodeMsPerTokenP95"],
      "peak_memory_mb": ["metrics.peak_memory_mb"]
    },
    "metadataPaths": {
      "version": ["env.version"],
      "model": ["model.id"]
    },
    "requiredMetrics": [
      "decodeTokensPerSec",
      "firstTokenMs"
    ]
  }
}
