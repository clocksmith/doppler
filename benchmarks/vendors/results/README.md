Normalized comparison result files are written here by `tools/compare-engines.js`.

- `results/*.json` are local/generated artifacts and are gitignored.
- Committed benchmark evidence JSON fixtures live under `benchmarks/vendors/fixtures/`.
- Committed charts (`*.svg`) may be kept here for README/docs snapshots.
